apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: picoclaw

resources:
  - namespace.yaml
  - configmap.yaml
  - pvc.yaml
  - gateway-deployment.yaml
  # Apply agent-job.yaml manually if necessary

commonLabels:
  app: picoclaw

images:
  - name: picoclaw
    newName: picoclaw
    newTag: latest
