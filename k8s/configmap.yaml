apiVersion: v1
kind: ConfigMap
metadata:
  name: picoclaw-config
  namespace: picoclaw
  labels:
    app: picoclaw
data:
  # Place the contents of config.json here
  # Example:
  # config.json: |
  #   {
  #     "key": "value"
  #   }
